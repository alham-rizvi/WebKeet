import { createFileRoute, Link, useNavigate } from '@tanstack/react-router';import { useState } from 'react';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';import { supabase } from '@/integrations/supabase/client';
export const Route=createFileRoute('/reset-password')({head:()=>({meta:[{title:'Reset password — WebKeet'},{name:'description',content:'Choose a new WebKeet password.'},{property:'og:title',content:'Reset your WebKeet password'},{property:'og:description',content:'Secure account recovery.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Page});function Page(){const[p,setP]=useState('');const[m,setM]=useState('');const nav=useNavigate();async function go(e:React.FormEvent){e.preventDefault();if(!window.location.hash.includes('type=recovery')){setM('Open the recovery link from your email.');return}const{error}=await supabase.auth.updateUser({password:p});if(error)setM(error.message);else await nav({to:'/dashboard'})}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={go} className="w-full max-w-md border border-border p-8"><h1 className="text-3xl font-semibold">Set a new password</h1><div className="mt-6"><Label htmlFor="new">New password</Label><Input id="new" type="password" minLength={8} className="mt-2" value={p} onChange={e=>setP(e.target.value)} required/></div>{m&&<p className="mt-4 text-sm">{m}</p>}<Button className="mt-6 w-full">Update password</Button><Link to="/auth" className="mt-5 block text-center text-sm underline">Back to sign in</Link></form></main>}