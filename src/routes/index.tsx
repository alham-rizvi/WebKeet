import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, CheckCircle2, Layers3, Play, Trophy } from "lucide-react";
import { Button } from "@/components/ui/button";
import { SiteShell } from "@/components/site-shell";

import { getPublicData } from "@/lib/webkeet.functions";
import { VistaIcon } from "@/components/vista-icon";
import { categoryMeta, DIFFICULTY_CLASS, DIFFICULTY_LABEL } from "@/lib/catalog";
import { Route as RouteIcon } from "lucide-react";

export const Route = createFileRoute("/")({
  loader: () => getPublicData(),
  head: () => ({ meta: [{ title: "WebKeet — Learn web security by doing" },{ name: "description", content: "Launch isolated OWASP WebGoat labs, practice real web security flaws, and track your progress." },{ property: "og:title", content: "WebKeet — Learn web security by doing" },{ property: "og:description", content: "Hands-on OWASP WebGoat labs with progress and leaderboards." },{ property: "og:type", content: "website" },{ name: "twitter:card", content: "summary_large_image" }] }),
  component: Home,
});

function Home() {
  const data = Route.useLoaderData();
  return <SiteShell><main>
    <section className="overflow-hidden border-b border-border">
      <div className="mx-auto grid min-h-[720px] max-w-7xl lg:grid-cols-[0.9fr_1.1fr]">
        <div className="flex flex-col justify-center px-5 py-20 lg:px-8">
          <p className="mb-6 font-mono text-xs font-semibold uppercase tracking-widest text-muted-foreground">Hands-on cybersecurity lab platform</p>
          <h1 className="max-w-2xl text-5xl font-semibold leading-[1.02] sm:text-7xl">Break the web.<br/><span className="text-muted-foreground">Build it safer.</span></h1>
          <p className="mt-7 max-w-lg text-lg leading-8 text-muted-foreground">Launch isolated, vulnerable-by-design labs across web, API, cloud, Linux, cryptography and forensics. Learn the exploit, understand the fix, and prove the skill.</p>
          <div className="mt-9 flex flex-wrap gap-3"><Button asChild size="lg"><Link to="/labs">Explore labs <ArrowRight /></Link></Button><Button asChild size="lg" variant="outline"><Link to="/auth" search={{ mode: "signup" }}>Create free account</Link></Button></div>
          <div className="mt-12 grid max-w-lg grid-cols-3 border-y border-border py-5">{[[data.stats.users,'Learners'],[data.stats.labs,'Labs'],[data.stats.completions,'Completed']].map(([n,l])=><div key={l} className="border-r border-border px-4 first:pl-0 last:border-0"><div className="text-2xl font-semibold">{n}</div><div className="text-xs text-muted-foreground">{l}</div></div>)}</div>
        </div>
        <div className="relative grid min-h-[480px] place-items-center overflow-hidden bg-foreground p-8 text-background">
          <div className="absolute inset-0 opacity-10" style={{backgroundImage:'linear-gradient(currentColor 1px,transparent 1px),linear-gradient(90deg,currentColor 1px,transparent 1px)',backgroundSize:'48px 48px'}} />
          <div className="relative w-full max-w-xl border border-background/20 bg-foreground p-6 shadow-2xl">
            <div className="mb-10 flex items-center justify-between border-b border-background/20 pb-4"><span className="font-mono text-xs">LAB / SQL INJECTION</span><span className="size-2 bg-signal" /></div>
            <img src="/illustrations/owasp.svg" alt="OWASP" className="mb-10 h-20 w-20 brightness-0 invert" />
            <p className="text-3xl font-semibold">SQL Injection Fundamentals</p><p className="mt-3 text-sm text-background/60">25 min · Beginner · 100 points</p>
            <div className="mt-10 flex items-center gap-4 border-t border-background/20 pt-5"><span className="grid size-11 place-items-center bg-background text-foreground"><Play className="size-4 fill-current" /></span><div><div className="text-sm font-semibold">Ready to launch</div><div className="text-xs text-background/50">Private 60-minute instance</div></div></div>
          </div>
        </div>
      </div>
    </section>
    <section id="how-it-works" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="grid gap-12 lg:grid-cols-[0.7fr_1.3fr]"><div><p className="font-mono text-xs text-muted-foreground">HOW IT WORKS</p><h2 className="mt-4 text-4xl font-semibold">Practice against the real thing.</h2></div><div className="grid gap-px bg-border sm:grid-cols-3">{([{Icon:Play,t:'Launch',d:'Pick a lab and spin up a private, time-boxed instance built from WebGoat, Juice Shop, DVWA, crAPI or one of our own images.'},{Icon:Layers3,t:'Exploit',d:'Read the scenario, follow three progressive hints if you need them, and exploit the flaw inside your own sandbox only.'},{Icon:CheckCircle2,t:'Complete',d:'Submit the flag or let the engine confirm completion. Points land once, the write-up unlocks, and the leaderboard updates.'}]).map(({Icon,t,d},i)=><div key={t} className="bg-background p-7"><span className="font-mono text-xs text-muted-foreground">0{i+1}</span><VistaIcon icon={Icon} tone={["green","amber","blue"][i]} size={40} className="mt-10"/><h3 className="mt-5 font-semibold">{String(t)}</h3><p className="mt-2 text-sm text-muted-foreground">{String(d)}</p></div>)}</div></div></section>
    <section className="border-y border-border bg-muted/30"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8">
      <div className="flex items-end justify-between"><div><p className="font-mono text-xs text-muted-foreground">CATEGORIES</p><h2 className="mt-2 text-3xl font-semibold">Ten tracks, well beyond OWASP.</h2></div><Button asChild variant="outline"><Link to="/labs">All {data.stats.labs} labs <ArrowRight /></Link></Button></div>
      <div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{data.categories.map(c=>{const m=categoryMeta(c.name);const n=data.labs.filter(l=>l.category===c.name).length;return <Link key={c.slug} to="/labs" className="rounded-md border border-border bg-card p-4 hover:border-primary"><VistaIcon icon={m.icon} tone={m.tone} size={38}/><h3 className="mt-3 text-sm font-semibold">{c.name}</h3><p className="mt-1 line-clamp-2 text-xs text-muted-foreground">{c.blurb}</p><p className="mt-2 font-mono text-[11px] text-muted-foreground">{n} labs</p></Link>})}</div>
      <h3 className="mt-14 text-xl font-semibold">Featured labs</h3>
      <div className="mt-4 overflow-hidden rounded-md border border-border bg-card"><table className="w-full text-sm"><thead className="bg-muted/60 text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-3 py-2 text-left">Title</th><th className="hidden px-3 py-2 text-left md:table-cell">Category</th><th className="px-3 py-2 text-left">Difficulty</th><th className="px-3 py-2 text-right">Points</th></tr></thead><tbody>{data.labs.filter((_,i)=>i%5===0).slice(0,8).map(l=>{const m=categoryMeta(l.category);return <tr key={l.id} className="border-t border-border hover:bg-accent/40"><td className="px-3 py-2"><Link to="/labs/$slug" params={{slug:l.slug}} className="flex items-center gap-2 font-medium hover:text-primary"><VistaIcon icon={m.icon} tone={m.tone} size={20}/>{l.title}</Link></td><td className="hidden px-3 py-2 text-muted-foreground md:table-cell">{l.category}</td><td className={`px-3 py-2 font-medium ${DIFFICULTY_CLASS[l.difficulty]}`}>{DIFFICULTY_LABEL[l.difficulty]}</td><td className="px-3 py-2 text-right font-mono text-xs">{l.points}</td></tr>})}</tbody></table></div>
      <div className="mt-14 flex items-end justify-between"><h3 className="text-xl font-semibold">Learn paths</h3><Link to="/learn-paths" className="text-sm text-muted-foreground hover:text-foreground">View all paths</Link></div>
      <div className="mt-4 grid gap-3 md:grid-cols-3 lg:grid-cols-5">{data.paths.map(p=><Link key={p.slug} to="/learn-paths" className="rounded-md border border-border bg-card p-4 hover:border-primary"><VistaIcon icon={RouteIcon} tone={p.level==='Advanced'?'red':p.level==='Intermediate'?'amber':'green'} size={32}/><h4 className="mt-3 text-sm font-semibold">{p.title}</h4><p className="mt-1 line-clamp-3 text-xs text-muted-foreground">{p.summary}</p><p className="mt-2 font-mono text-[11px] text-muted-foreground">{p.level} · {p.labs.length} labs</p></Link>)}</div>
    </div></section>
    <section className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="grid items-center gap-12 lg:grid-cols-2"><div className="grid grid-cols-3 items-center gap-7 border border-border p-10">{['docker','openjdk','postgresql','html5','jsonwebtokens','owasp'].map(x=><img key={x} src={`/illustrations/${x}.svg`} alt={x} className="mx-auto max-h-16 max-w-20 grayscale dark:invert"/>)}</div><div><Trophy className="size-8"/><h2 className="mt-6 text-4xl font-semibold">Learn in public. Climb the board.</h2><p className="mt-5 max-w-lg text-muted-foreground">Every verified completion earns points. Finish every published lab to earn the All Labs badge.</p><Button asChild className="mt-8" variant="outline"><Link to="/hall-of-fame">Hall of Fame <ArrowRight /></Link></Button></div></div></section>
    <section className="mx-auto max-w-4xl px-5 py-20 lg:px-8"><p className="font-mono text-xs text-muted-foreground">FAQ</p><h2 className="mt-2 text-3xl font-semibold">Questions people ask first</h2><div className="mt-8 divide-y divide-border rounded-md border border-border"><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Is it legal to attack these labs?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">Yes, inside the instance WebKeet launches for you. Every target is deliberately vulnerable and runs in a private sandbox. Attacking anything else — including WebKeet itself — breaks the Acceptable Use Policy and possibly the law.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Do I need to install anything?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">No. Labs run in the browser or through the instance URL. Some labs suggest tools like Burp Suite, nmap or Python, and the lab page lists exactly what helps.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">How long does an instance live?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">Sixty minutes. You can stop it early or reset it for a clean copy. One active instance per account keeps capacity fair.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">How is completion verified?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">By a flag you submit or a callback from the lab engine. Points are awarded once per lab, and the solution write-up unlocks afterwards.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Which engines power the labs?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">OWASP WebGoat, OWASP Juice Shop, DVWA, OWASP NodeGoat, crAPI, and small custom Docker images written by the WebKeet team for tracks that have no upstream project.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Is WebKeet free?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">Creating an account and working through the catalog is free. We may add paid team features later, but solo learners keep full access.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">I am a complete beginner. Where do I start?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">Open the Web Application Security Fundamentals path. It begins with SQL injection and cross-site scripting and explains the fix alongside every exploit.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Can I contribute a lab?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">Yes. Every lab stores an engine, a Docker image and an author, so community labs slot in like any other. Reach out through the Contact page with your idea.</p></details><details className="group p-4"><summary className="cursor-pointer list-none font-medium">Why are some solutions locked?</summary><p className="mt-2 text-sm leading-7 text-muted-foreground">So you get the learning moment first. Finish the lab and the full write-up, including the code-level fix, becomes available.</p></details></div></section>
  </main></SiteShell>;
}